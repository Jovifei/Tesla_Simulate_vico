# S12 Simulink Sound Playground v0.9 v4 — Untracked Scope Audit

This repository already contains user-owned untracked S12 review/report files. The v4 audit payload is intentionally limited to:

- `tools/sound_sim/s12/playground/` text source and audit manifests;
- the five named Sound Playground test files;
- the v4 plan and v4 offline report;
- two separately named read-only SLX evidence files;
- package-local Git/status and self-test records.

Excluded from the v4 payload: the repository history, `slprj`, cache, generated WAV/PCM, raw recordings, credentials, APKs, and unrelated untracked reports. The workspace `.slx` is copied only into the separately named `evidence/` role and is not included in `source/`.
