# docs/superpowers — Historical Design Archive

状态：`HISTORICAL_SNAPSHOT_INDEX`

`plans/` 和 `specs/` 保存 2026-08 的阶段性设计/实施计划，用于追溯 Stage W/X/Y/R2 的决策过程。

它们**不是当前产品或执行 authority**。如果其中内容与 current docs 冲突，以：

```text
current user decision
→ current GitHub truth
→ docs/00-reference authority
→ current status/architecture/requirements/roadmap
```

为准。

不要直接从旧 Superpowers plan 恢复 ESP32、旧 R2 调参或旧 Stage-Y 阶段门；先读取 `docs/README.md` 和当前 status。
