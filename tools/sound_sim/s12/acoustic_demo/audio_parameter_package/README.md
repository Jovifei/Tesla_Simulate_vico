# AudioParameterPackage v0.1

This package is a JSON-only boundary for future Android, ESP32, or DSP consumers.
It contains no Python objects, recordings, OEM calibration, or real-time implementation.
`source_commit`, package `hash`, C/synthetic `provenance`, and the frozen radiation package SHA make each export auditable; consumers must reject any hash mismatch.
