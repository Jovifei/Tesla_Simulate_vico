# S12 v1.2 shared acoustic adapter model

This folder contains the shared Simulink model that applies the immutable
Sprint 4D-B radiation package through the v1.2 stateful audio adapter. It is
not the complete FVM/PTR network.
