# S12 Stage P Fixture-Only Stage-O Consumption

Status: `FIXTURE_ONLY_NOT_HUMAN_FEEDBACK_NOT_TUNING_AUTHORITY`. Accepted `8` synthetic rows through the importer semantics; human feedback: `False`; tuning authority: `False`.

The same official browser pair was passed through the Stage-O entry boundary and returned `REJECTED_FIXTURE_OR_SYNTHETIC_INPUT` with `0` accepted rows.

The derived confusion matrix and per-vehicle scores are explicitly fixture-only evidence. They cannot qualify Stage O, produce HUMAN_PASS, or authorize a sound-fix/profile-freeze branch.
