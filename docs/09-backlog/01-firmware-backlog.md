# Tesla Simulate Vico ESP32 固件待完成清单（Deferred / Historical）

> `DEFERRED_FUTURE_OPTION`。不是当前 active backlog。

历史待办：flash/boot、BLE、SD、I2S、input/UI、WiFi/MQTT/OTA、IRAM/PSRAM、CAN analyser、ESP32 sound adapter、hardware audio、vehicle pilot——全部暂停。

当前 active backlog：`docs/09-backlog/02-project-master-backlog.md`。

```text
AC8
→ Jovi Hellcat V3
→ Engineering Profile
→ Ferrari/RX-7
→ speed+acceleration VirtualEngineState
→ AudioParameterPackage
→ C++
→ Android App
→ in-car validation
```

只有用户明确重新开启嵌入式版本时才恢复本列表。