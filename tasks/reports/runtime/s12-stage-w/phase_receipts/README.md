# Stage W phase receipts

W9 has the dedicated final qualification JSON receipt. Earlier phases are bound through `execution_state.json` together with their source-backed evidence paths and commits; they do not each have a dedicated JSON receipt. Receipts contain only source-backed commands, hashes, statuses, and evidence paths; raw external media stays outside Git.
